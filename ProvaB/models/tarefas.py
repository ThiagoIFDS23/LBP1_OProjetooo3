def add_Tarefa(tarefas, id, titulo, descricao, vencimento):
    tarefas.append({
        "id": id,
        "titulo": titulo,
        "descricao": descricao,
        "vencimento": vencimento
        })